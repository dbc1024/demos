package com.ectrip.ticket.service.service;

import com.alibaba.fastjson.JSONObject;
//import com.ectrip.bank.action.EndorderAction;

/**
 * 结算中心，加调WEB SERVICE
 * @author lijin
 *
 */
public class EndOrderPayService {

    /*public int PayOrderService(String xmldata,String md5) throws Exception

    {

        EndorderAction ea = new EndorderAction();
        String rc_ac = ea.PayOrderService(xmldata, md5);
        JSONObject jsonObject = JSONObject.parseObject(rc_ac);
        String code = jsonObject.getString("code").toString();

		*//*String usid = jsonObject.getString("usid").toString();
		String payid = jsonObject.getString("payid").toString();
		String orid = jsonObject.getString("orid").toString();
		String amount	= jsonObject.getString("amount").toString();
	*//*

        if (code.equals("1") ){
            return 1;
        } else {
            return -1;
        }
    }*/
}
